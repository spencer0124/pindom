import { ScreenPlaceholder } from '@/components/ScreenPlaceholder';

export default function MyScreen() {
  return (
    <ScreenPlaceholder
      title="마이페이지"
      node="33:1597"
      note="프로필·설정"
      next={[]}
    />
  );
}
